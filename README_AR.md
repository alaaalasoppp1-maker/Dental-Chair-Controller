# Controller 3.7.0 update

انسخ محتويات هذه الحزمة فوق مجلد `controller` الإصدار 3.6.1 أو فوق مستودعك الحالي واختر overwrite. لا تحذف مجلد `assets/fonts` الموجود عندك؛ هذا التحديث لا يغيره. بعد الدمج شغّل `npm ci`, ثم `npm run check`, ثم `npm test`, ثم `npm run dist:win`.
